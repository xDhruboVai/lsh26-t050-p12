/**
 * The demo account.
 *
 * The problem asks for accounts; the rulebook asks for a live URL that needs no
 * setup. Both hold if the login screen offers a real, pre-seeded account a
 * judge can enter with one click, while everyone else registers normally.
 */
export const DEMO_EMAIL = 'demo@ledger.app';
export const DEMO_PASSWORD = 'demo-ledger-2026';
