node_modules
.next
out
.vercel
*.tsbuildinfo
next-env.d.ts
.claude

# Secrets. Ignore every env file, then allow back only the empty template.
# `.env*.local` alone does not match a plain `.env`.
.env*
!.env.example

.vercel
.env*
